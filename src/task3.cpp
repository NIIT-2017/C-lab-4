#include <stdio.h>
#include <stdlib.h>
# include <string.h>

int isPalindrome(char* str) {
    char* string[128];
    char* toTheBeginig = str;
    char* toTheEnd;
    int i = 0;
    int j = 0;
    int result;

    while (*str) {
        str++;
    }
    str--;
    toTheEnd = str;

    while (toTheBeginig < toTheEnd) {
    
    
    }
    
        if (toTheBeginig != toTheEnd) {
            result = 0;
            } else if ()
    
    }
    return result;
}
